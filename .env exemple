# Fichier d'exemple .env
# Remplace les valeurs et crée un fichier .env local avant de démarrer

# Préfix pour les commandes, ex: !help
PREFIX=!

# Chemin du fichier de session (auth) — ne pas versionner
SESSION_FILE=./auth_info_multi.json

# Optionnel : ton numéro ou code parrainé par défaut (utilisé dans des messages ou commandes)
PARAINE_CODE=

# Si tu veux envoyer des messages automatisés depuis le bot, définis le numéro de destination (format WhatsApp: 33612345678@s.whatsapp.net)
DEFAULT_TARGET_NUMBER=

# Mode debug
LOG_LEVEL=info
